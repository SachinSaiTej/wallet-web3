import React from 'react'

const EthereumWallet = ({menmonic}:{mnemonic:string}) => {
  return (
    <div style={{ display: "flex", flexDirection: "column" }}>
        <button>Add Ethereum Wallet</button>
    </div>
  )
}

export default EthereumWallet